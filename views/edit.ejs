<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3>Edit your chats :<%= chat._id %></h3>
    <p>This chat is sent from <%= chat.from %> to <%= chat.to %></p>
    <form method="POST" action="/chats/<%= chat._id %>?_method=PUT">
        <textarea name="msg" id="1"><%= chat.msg %></textarea><br>
        <button>Edit</button>
    </form>
</body>
</html>